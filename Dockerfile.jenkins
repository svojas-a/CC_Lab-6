FROM jenkins/jenkins:lts

USER root

# Install Docker CLI and required build tools
RUN apt-get update && apt-get install -y \
    docker.io \
    make \
    g++ \
    curl \
 && apt-get clean

USER jenkins